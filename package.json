{
  "name": "book-api",
  "version": "1.0.0",
  "description": "This repository contains a RESTful API built with Node.js and Express, designed to manage book records. The API allows users to perform CRUD (Create, Read, Update, Delete) operations on book records, with authentication and authorization managed through JSON Web Tokens (JWT).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
